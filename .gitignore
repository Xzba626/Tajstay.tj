node_modules/
.next/
dist/
coverage/
*.log

.env
.env.local
.env.*.local

*.tsbuildinfo

# Legacy SQLite (project uses PostgreSQL only)
*.db
*.db-journal
prisma/dev.db
prisma/dev.db-journal
prisma/*.db
prisma/*.db-journal

# Local tunnel binary (do not deploy)
cloudflared.exe

# Legacy admin secret word file (migrated to AdminSecurityState in DB)
data/admin-security.json
# Legacy JSON stores (migrated to SiteContentState / OwnerPaymentMethod in DB)
data/site-content.json
data/owner-payment-methods.json
